{
  "name": "surge-rule",
  "version": "0.1.0",
  "private": true,
  "description": "Unified proxy routing rule sets.",
  "scripts": {
    "build": "node scripts/build.js",
    "lint": "node scripts/lint.js"
  }
}

